const fs=require("node:fs"),path=require("node:path"),root=process.cwd(),read=s=>fs.readFileSync(path.join(root,s),"utf8"),exists=s=>fs.existsSync(path.join(root,s)),escapeRegExp=s=>s.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),failures=[],scripts=JSON.parse(read("package.json")).scripts||{};for(const s of["build:css","build:tailwind","build:tailwind:watch"])Object.prototype.hasOwnProperty.call(scripts,s)&&failures.push(`Starter package.json must not define \`${s}\`; build ownership belongs to gem repos.`);const config=read("_config.yml");for(const s of["al_folio_core","al_folio_distill","al_cookie","al_icons","al_math"])new RegExp(`^\\s*-\\s*${escapeRegExp(s)}\\s*$`,"m").test(config)||failures.push(`\`_config.yml\` plugins must include \`${s}\`.`);/^\s*theme:\s*al_folio_core\s*$/m.test(config)||failures.push("`_config.yml` must keep `theme: al_folio_core` for thin-starter wiring.");for(const s of["fontawesome","academicons","scholar-icons"])new RegExp(`^\\s{2}${escapeRegExp(s)}:\\s*$`,"m").test(config)||failures.push(`\`_config.yml\` must define \`third_party_libraries.${s}\`.`);for(const s of["_includes","_layouts","_sass","_scripts","assets/tailwind","tailwind.config.js","assets/webfonts"])exists(s)&&failures.push(`Starter must not own core component path \`${s}\`.`);failures.length>0&&(failures.forEach(()=>{}),process.exit(1));